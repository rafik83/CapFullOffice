 <form name="login_form" class="form-signin console-form-auth" role="form" action="{#{ path('user_homepage') }#}" method="post">
                            <div class="panel-body bg-light p25 pb15">
                                <div class="section">
                                    <label class="field-label text-muted fs14 mb10" for="login_form__username">Username</label>
                                    <label for="username" class="field prepend-icon">
                                        <input type="text" id="login_form__username" name="login_form[_username]" required="required"  placeholder="Username"  class="form-control" required autofocus>
                                        <label for="username" class="field-icon">
                                            <i class="fa fa-user"></i>
                                        </label>
                                    </label>

                                </div>

                                <div class="section">
                                    <label for="login_form__password" class="field-label text-muted fs14 mb10">Mot de passe </label>
                                    <label for="password" class="field prepend-icon">
                                        <input type="password" id="login_form__password" class="form-control" name="login_form[_password]" placeholder="Mot de passe"  style="margin:0px;"  required>
                                        <label for="password" class="field-icon">
                                            <i class="fa fa-lock"></i>
                                        </label>
                                    </label>
                                </div>


                                <!--input type="hidden" id="get_uri_redirect" value="" -->


                                <!--a href="">Register</a-->
                            </div>
                            <input type="hidden" name="_csrf_token"
                                   value="{{ csrf_token('authenticate') }}"
                                   >
                            <input type="hidden" id="login_form__token" name="login_form[_token]" value="{{token}}">

                            <div class="panel-footer clearfix">
                                <button type="submit" class="button btn-primary mr10 pull-right">Connexion</button>
                                &nbsp;
                            </div>
                        </form>












 {{ form_start(form) }}
                        {{ form_row(form._username) }}
                        {{ form_row(form._password) }}
                        <input type="hidden" name="_csrf_token"
                               value="{{ csrf_token('authenticate') }}"
                               >
                         <button type="submit" class="button btn-primary mr10 pull-right">Connexion</button>
                          &nbsp;

                        {{ form_end(form) }}
